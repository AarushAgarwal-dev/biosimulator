fastapi>=0.100.0
# Pin Starlette to the range FastAPI supports. A newer Starlette (>=0.51)
# breaks app startup: "Router.__init__() got an unexpected keyword argument 'on_startup'".
starlette>=0.40.0,<0.51.0
uvicorn>=0.22.0
numpy>=1.24.0
scipy>=1.10.0
sympy>=1.12
pydantic>=2.0.0
requests>=2.31.0

# Open-source LLM: in-process GGUF inference on CPU + HuggingFace model downloads.
# (Replaces the previous Google Gemini dependency.) For a prebuilt CPU wheel use:
#   pip install llama-cpp-python --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cpu
llama-cpp-python>=0.3.0
huggingface_hub>=0.23.0

boto3>=1.34.0
awscrt>=0.19  # AWS SSO/login credential provider (botocore[crt])
python-dotenv>=1.0.0
